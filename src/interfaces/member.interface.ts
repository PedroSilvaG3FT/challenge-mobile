export default interface MemberInterface {
    id?: number
    email: string,
    name: string,
    password?: string,
    image: string,
    startingWeight: number,
    goalWeight: number,
    height: number,
    payday: number,
}